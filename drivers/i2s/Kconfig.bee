# BEE i2s configuration

# Copyright(c) 2025, Realtek Semiconductor Corporation.
# SPDX-License-Identifier: Apache-2.0

menuconfig I2S_BEE
	bool "BEE MCU i2s controller driver"
	default y
	depends on DT_HAS_REALTEK_BEE_I2S_ENABLED
	select DMA
	select USE_HAL_REALTEK_I2S
	help
	  Enable the bee i2s driver.

if I2S_BEE

config I2S_BEE_RX
	bool "I2S RX"
	default n

if I2S_BEE_RX

config I2S_BEE_RX_BLOCK_COUNT
	int "RX queue length"
	default 4

endif # I2S_BEE_RX

config I2S_BEE_TX
	bool "I2S TX"
	default n

if I2S_BEE_TX

config I2S_BEE_TX_BLOCK_COUNT
	int "TX queue length"
	default 4

endif # I2S_BEE_TX

endif # I2S_BEE
